//
// Created by Julio Anzaldo on 4/28/22.
//

#ifndef GUIPROJECT_ENUMNAMES_H
#define GUIPROJECT_ENUMNAMES_H

enum FontNames {
    ARIAL = 0, YAAHOWU = 1, AGENT = 2,

    LASTFONT = 3
};

enum WindowMenuNames {
    WINDOW, RUN, CLOSE, THEME,

    LASTWINDOW
};

enum ImageNames {
    EQUATIONS,

    LASTIMAGE
};

enum ScreenModes
{
    LIGHTMODE, DARKMODE,

    LASTMODE
};

enum State {
    HOVERED, HIDDEN, CLICKED, PLAYED, LEFT, RIGHT,

    LASTSTATE
};

enum ScreenStates {
    SCREEN1, SCREEN2, SCREEN3,

    LASTSCREEN
};

#endif //GUIPROJECT_ENUMNAMES_H
